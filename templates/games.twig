<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Games - Game Library</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Style CSS -->
    <link rel="stylesheet" href="css/style.css">
</head>
<body>  

<!-- Navigation bar -->
<nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="dashboard.php"> Game Library</a>
        <div>
            <a href="dashboard.php" class="btn btn-sm btn-outline-light me-2">Dashboard</a>
            <a href="search_games.php" class="btn btn-sm btn-outline-light me-2">Search</a>
            <a href="add_game.php" class="btn btn-sm btn-outline-light me-2">Add Game</a>
            <a href="logout.php" class="btn btn-sm btn-outline-light">Logout</a>
        </div>
    </div>
</nav>

<!-- Main content Page -->
<div class='container mt-5'>
    <h1>My Games</h1>

    <a href="add_game.php" class="btn btn-primary mb-3">Add New Game</a>

    <!-- Games Table -->
    <table class="table table-striped">
        <thead class="table-dark">
            <tr>
                <th>Title</th>
                <th>Genre</th>
                <th>Platform</th>
                <th>Release Date</th>
                <th>Rating</th>
                <th>Hours Played</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for game in games %}
            <tr>
                <td>{{ game.title }}</td>
                <td>{{ game.genre }}</td>
                <td>{{ game.platform }}</td>
                <td>{{ game.release_date }}</td>
                <td>{{ game.rating }}</td>
                <td>{{ game.hours_played|number_format(0) }}</td>           
                <td>{{ game.status }}</td>
                <td>
                    <a href="edit_game.php?id={{ game.id }}" class="btn btn-sm btn-warning">Edit</a>
                    <a href="delete_game.php?id={{ game.id }}" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this game?');">Delete</a>
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="8">No games have been added. Please try add games.</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>